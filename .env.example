# Copy to .env and paste your key from https://302.ai — used only by npm run gen:assets (never shipped to GitHub Pages).
AI302_API_KEY=

# Music: in tools/assets.config.json set "skip": false on entries under "music" to generate .mp3 via Suno (paid per call).
